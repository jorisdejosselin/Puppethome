version: '3'
services:
  frontend:
    ports:
    - "80:80"
    container_name: frontend
  redis:
    image: <%= @imgfront %>
  backend:
    container_name: backend
     ports:
    - "80:80"
  redis:
    image: <%= @imgback %>
