[Unit]
Description=For deploying PHP update IP script
Wants=basic.target
After=basic.target network.target

[Service]
WorkingDirectory=/mnt/data/scripts/transip
ExecStart=/usr/bin/php CheckWAN.php

[Install]
WantedBy=multi-user.target
